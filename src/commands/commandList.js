const CommandList = (client, channel) => {
  client.say(channel, 'List of commands available: ping , pyramid , hmm , afk , time , chatting , higherorlower (hol). Type !help <cmd> to get full details on specific command.')
}

export default CommandList